import 'package:flutter/material.dart';

class RankCell extends StatelessWidget {
  Map _rankInfo;

  RankCell(this._rankInfo);

  @override
  Widget build(BuildContext context) {
    return GestureDetector(
      child: Container(
        child: Row(

        ),
      ),
    );
  }
}
